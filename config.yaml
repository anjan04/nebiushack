# AutoRobot Configuration

# LLM settings (Nebius Token Factory)
llm:
  base_url: "https://api.studio.nebius.ai/v1/"
  api_key_env: "NEBIUS_API_KEY"
  model: "deepseek-ai/DeepSeek-V3-0324"
  fallback_model: "meta-llama/Llama-3.3-70B-Instruct"
  temperature: 0.7
  max_tokens: 4096

# Training settings
training:
  script: "train_loco.py"  # LocoMuJoCo with LAFAN1 mocap tracking
  time_budget_seconds: 180  # 3 minutes per experiment
  num_envs: 2048
  device: "cuda:0"
  headless: true
  max_iterations: 3000  # PPO iterations (within time budget)

# Environment
env:
  name: "UnitreeH1"
  mocap: "walk1_subject1"
  primary_metric: "primary_score"
  eval_episodes: 20

# Agent loop settings
agent:
  max_iterations: 100
  retry_on_error: 3
  stagnation_threshold: 5  # inject diversity after N iterations without improvement
  log_dir: "logs"
  results_dir: "results"

# Git settings
git:
  auto_commit: true
  tag_improvements: true
  branch: "experiments"
